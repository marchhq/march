'use clent'

import { useModal } from '@/src/contexts/ModalProvider'
import React from 'react'

type Props = {}

const MoveInboxItem = (props: Props) => {
   
  return (
    <div>MoveInboxItem</div>
  )
}

export default MoveInboxItem